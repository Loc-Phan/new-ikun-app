import Client from './client';
import NewClient from './newClient';
import {config, getApiUrl} from './config';
import {setToken} from './newConfig';

export {Client, NewClient, config, setToken, getApiUrl};
